This project is a version of the popular street fighter game. To install and run this project you will have to install the module pygame. To install pygame go to pygame.org and install it for either mac or windows. All the pictures required for this program are in the folder. You will have to change the the file paths in the code. 
